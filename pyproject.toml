[project]
name = "atic-cbfs"
version = "0.1.0"
description = "Differential-drive planning with CasADi"
requires-python = ">=3.10"
dependencies = [
    "casadi>=3.6",
    "matplotlib>=3.10.9",
    "numpy>=1.24",
    "pandas>=2.3.3",
]
